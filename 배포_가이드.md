# 🚀 소정초등학교 스마트 예약 시스템 배포 가이드

> **목표**: 내 컴퓨터에만 있는 앱을 → 인터넷 주소(URL)로 누구나 접속 가능하게 만들기
> **비용**: 전부 무료 🎉
> **소요 시간**: 약 20~30분

---

## 📦 준비물 확인

배포에 필요한 파일 목록이에요. 아래 파일들이 모두 있어야 해요:

```
school-reservation-deploy/
├── package.json          ← 프로젝트 설정
├── vite.config.js        ← 빌드 설정
├── vercel.json           ← Vercel 배포 설정
├── index.html            ← 시작 페이지
├── .gitignore            ← Git 제외 파일 목록
└── src/
    ├── main.jsx          ← React 진입점
    └── App.jsx           ← 앱 전체 코드 (Supabase 연결 포함)
```

---

## STEP 1 · GitHub 가입 및 저장소 만들기

GitHub은 코드를 저장하는 클라우드예요. Vercel이 여기서 코드를 가져와 배포해요.

### 1-1. GitHub 가입

1. **github.com** 접속
2. **Sign up** 클릭
3. 이메일, 비밀번호, 사용자 이름 입력 후 가입
4. 이메일 인증 완료

### 1-2. 새 저장소(Repository) 만들기

1. 로그인 후 오른쪽 상단 **＋** 버튼 클릭
2. **New repository** 선택
3. 아래와 같이 입력:

| 항목 | 입력값 |
|------|--------|
| Repository name | `school-reservation` |
| Description | 소정초등학교 스마트 예약 시스템 |
| Public/Private | **Public** 선택 |
| Initialize README | 체크 **안 함** |

4. **Create repository** 클릭

---

## STEP 2 · 파일 업로드하기

### 방법 A: 웹 브라우저에서 직접 업로드 (권장 - 쉬움)

1. 생성된 저장소 페이지에서 **"uploading an existing file"** 링크 클릭
2. 파일 전체를 **드래그 앤 드롭**으로 올리기

> ⚠️ 주의: `node_modules` 폴더는 올리지 마세요! (없으면 괜찮아요)

올려야 할 파일:
- `package.json`
- `vite.config.js`
- `vercel.json`
- `index.html`
- `.gitignore`
- `src/` 폴더 (main.jsx, App.jsx 포함)

3. 하단 **Commit changes** 클릭

### 방법 B: GitHub Desktop 앱 사용 (선택)

1. **desktop.github.com** 에서 GitHub Desktop 설치
2. File → Clone Repository → 만든 저장소 선택
3. 파일을 해당 폴더에 복사
4. 변경사항 Commit & Push

---

## STEP 3 · Vercel 가입 및 배포

Vercel은 GitHub 코드를 자동으로 빌드하고 URL을 만들어줘요.

### 3-1. Vercel 가입

1. **vercel.com** 접속
2. **Sign Up** 클릭
3. **Continue with GitHub** 선택 (GitHub 계정으로 바로 가입!)
4. 권한 허용

### 3-2. 프로젝트 배포

1. Vercel 대시보드에서 **"Add New Project"** 클릭
2. GitHub 저장소 목록에서 **school-reservation** 선택
3. **Import** 클릭
4. 아래 설정 확인:

| 항목 | 설정값 |
|------|--------|
| Framework Preset | **Vite** |
| Build Command | `npm run build` |
| Output Directory | `dist` |

5. **Deploy** 클릭!
6. 1~2분 기다리면 배포 완료 🎉

### 3-3. URL 확인

배포가 완료되면 이런 URL이 생겨요:

```
https://school-reservation-abc123.vercel.app
```

이 URL을 선생님들과 공유하면 바로 접속할 수 있어요!

---

## STEP 4 · 배포 후 테스트

URL에 접속해서 아래 항목을 테스트해보세요:

- [ ] 로그인 화면이 보임
- [ ] teacher1 / 1234 로 로그인 성공
- [ ] 시설 목록이 Supabase에서 불러와짐
- [ ] 예약 신청 후 Supabase Table Editor에 저장 확인
- [ ] admin / 0000 으로 관리자 탭 접근

---

## STEP 5 · 도메인 연결 (선택 · 나중에)

기본 URL이 너무 길다면 짧은 도메인을 연결할 수 있어요:

```
기본: school-reservation-abc123.vercel.app
커스텀: sojeong-reservation.com  ← 유료 (연 1~2만원)
무료: sojeong.vercel.app         ← Vercel 서브도메인 변경 가능
```

Vercel 대시보드 → Project Settings → Domains 에서 설정

---

## ❓ 자주 묻는 오류

### "Build Failed" 오류

```
원인: package.json 또는 vite.config.js 파일 누락
해결: 파일이 모두 올바르게 업로드됐는지 확인
```

### 로그인 오류 (배포 후에도)

```
원인: Supabase CORS 설정 필요
해결: Supabase 대시보드 → Authentication → URL Configuration
      → Site URL에 Vercel URL 추가
      예: https://school-reservation-abc123.vercel.app
```

### 데이터가 안 보임

```
원인: Supabase RLS(보안) 설정
해결: SQL Editor에서 아래 실행
      alter table facilities   disable row level security;
      alter table items        disable row level security;
      alter table users        disable row level security;
      alter table reservations disable row level security;
```

---

## 📱 선생님들에게 배포하는 방법

배포가 완료되면 URL을 이렇게 공유하세요:

1. **카카오톡 단체방**에 URL 공유
2. **학교 홈페이지** 링크 등록
3. **QR코드** 만들어서 교무실에 부착
   - qr.io 또는 qrcode-generator.de 에서 무료 생성

---

## 🔄 코드 수정 후 재배포

앱을 수정하고 싶을 때:

1. GitHub에서 파일 수정 (또는 새 파일 업로드)
2. Commit 하면 → **Vercel이 자동으로 재배포!**
3. 1~2분 후 변경사항 반영 완료

---

## 📞 도움이 필요하면

문제가 생기면 오류 화면을 스크린샷 찍어서
Claude에게 보여주세요. 바로 해결해드릴게요! 😊

---

*작성일: 2026년 4월 | 소정초등학교 스마트 예약 시스템 v1.0*
